* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Cairo', sans-serif;
}

body {
    background-color: #0b0c10;
    color: #c5c6c7;
    scroll-behavior: smooth;
}

/* القائمة العلوية */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 5%;
    background-color: #12141c;
    position: fixed;
    width: 100%;
    top: 0;
    z-index: 1000;
    box-shadow: 0 4px 20px rgba(0,0,0,0.6);
    border-bottom: 1px solid #1f2833;
}

.logo {
    color: #ff4d00;
    font-size: 22px;
    font-weight: bold;
    letter-spacing: 2px;
}

nav ul {
    display: flex;
    list-style: none;
    align-items: center;
}

nav ul li {
    margin-left: 15px;
}

nav ul li a {
    color: #ffffff;
    text-decoration: none;
    font-size: 15px;
    transition: 0.3s;
}

nav ul li a:hover {
    color: #ffbd59;
}

/* الأزرار والقوائم */
.lang-dropdown {
    background-color: #1f2833;
    color: #ffbd59;
    border: 1px solid #ffbd59;
    padding: 4px 8px;
    border-radius: 5px;
    cursor: pointer;
}

/* الواجهة الرئيسية */
.hero {
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    background: radial-gradient(circle, rgba(20,15,10,0.75) 0%, rgba(11,12,16,1) 100%), url('https://unsplash.com');
    background-size: cover;
    background-position: center;
    padding: 120px 20px 60px 20px;
}

.guild-logo {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    border: 4px solid #ff4d00;
    box-shadow: 0 0 25px #ff4d00;
}

.hero-content h1 { font-size: 42px; color: #fff; margin-bottom: 15px; }
.highlight { color: #ff4d00; text-shadow: 0 0 15px #ff4d00; }
.hero-content p { font-size: 18px; max-width: 650px; margin: 0 auto 35px auto; }

.cta-button {
    display: inline-block;
    padding: 12px 35px;
    background: linear-gradient(45deg, #ff4d00, #ffbd59);
    color: #fff;
    text-decoration: none;
    font-weight: bold;
    border-radius: 25px;
    transition: 0.3s;
    border: none;
    cursor: pointer;
}
.cta-button:hover { transform: scale(1.05); box-shadow: 0 0 20px #ff4d00; }

/* أقسام الموقع المشتركة */
.rules-section, .roster-section, .apply-section, .shop-section, .media-section, .chat-section {
    padding: 80px 8%;
    text-align: center;
}
.rules-section { background-color: #0b0c10; }
.roster-section, .shop-section, .chat-section { background-color: #12141c; }
.apply-section, .media-section { background-color: #0b0c10; }

h2 { color: #fff; font-size: 30px; margin-bottom: 40px; }
.section-desc { color: #ffbd59; margin-bottom: 40px; font-size: 15px; }

/* شبكة القوانين */
.rules-grid { display: flex; justify-content: center; flex-wrap: wrap; gap: 20px; }
.rule-card { background-color: #12141c; padding: 30px; border-radius: 10px; width: 300px; border-top: 4px solid #ff4d00; }

/* جدول الأعضاء الاحترافي */
.table-container { overflow-x: auto; max-width: 900px; margin: 0 auto; }
table { width: 100%; border-collapse: collapse; background-color: #0b0c10; border-radius: 8px; overflow: hidden; }
th, td { padding: 15px; text-align: center; border-bottom: 1px solid #1f2833; }
th { background-color: #ff4d00; color: white; font-weight: bold; }
.rank-leader { color: #ffbd59; font-weight: bold; }
.rank-deputy { color: #ff4d00; font-weight: bold; }

/* نموذج التسجيل للتقديم */
.apply-form { max-width: 550px; margin: 0 auto; display: flex; flex-direction: column; gap: 15px; }
.apply-form input, .apply-form textarea {
    padding: 12px; background-color: #12141c; border: 1px solid #1f2833; border-radius: 6px; color: #fff; outline: none; text-align: right;
}
html[dir="ltr"] .apply-form input, html[dir="ltr"] .apply-form textarea { text-align: left; }
.apply-form input:focus, .apply-form textarea:focus { border-color: #ff4d00; }

/* متجر الأدوات */
.shop-grid { display: flex; justify-content: center; flex-wrap: wrap; gap: 30px; }
.item-card { background-color: #0b0c10; border: 1px solid #1f2833; border-radius: 12px; padding: 25px; width: 280px; transition: 0.3s; }
.item-card:hover { transform: translateY(-5px); border-color: #ffbd59; }
.item-icon { font-size: 40px; margin-bottom: 15px; }
.item-card h3 { color: #fff; margin-bottom: 10px; }
.item-desc { font-size: 13px; color: #888; margin-bottom: 15px; }
.price { color: #ffbd59; font-weight: bold; font-size: 18px; margin-bottom: 15px; }
.buy-btn { background-color: #ff4d00; border: none; color: white; padding: 8px 20px; border-radius: 4px; cursor: pointer; font-weight: bold; width: 100%; transition: 0.3s; }
.buy-btn:hover { background-color: #ffbd59; color: #0b0c10; }

/* الفيديوهات */
.video-grid { display: flex; justify-content: center; flex-wrap: wrap; gap: 30px; }
.video-card { background-color: #12141c; border-radius: 12px; overflow: hidden; width: 45%; min-width: 310px; }
.video-wrapper { position: relative; padding-bottom: 56.25%; height: 0; }
.video-wrapper iframe { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }
.video-card h3 { padding: 15px; color: #fff; }

/* صندوق الدردشة */
.chat-container { max-width: 800px; margin: 0 auto; background-color: #0b0c10; border-radius: 12px; overflow: hidden; border: 1px solid #ff4d00; box-shadow: 0 0 20px rgba(255, 77, 0, 0.2); }

footer { text-align: center; padding: 25px; background-color: #0b0c10; border-top: 1px solid #12141c; font-size: 13px; }

/* اللغات الأجنبية والهواتف */
html[dir="ltr"] * { font-family: 'Poppins', sans-serif; }
html[dir="ltr"] nav ul li { margin-left: 0; margin-right: 15px; }

@media (max-width: 900px) {
    header { flex-direction: column; padding: 15px; }
    nav ul { margin-top: 15px; flex-wrap: wrap; justify-content: center; gap: 8px; }
    nav ul li { margin: 0 5px !important; }
    .hero { padding-top: 240px; }
    .video-card { width: 100%; }
}
